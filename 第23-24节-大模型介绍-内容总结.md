# 大模型介绍 - 要点梳理


---

## 1. 核心概念：大模型
* **定义**：大模型通常指**参数规模 $\ge $ 百亿级（10B）**、在大规模数据上训练、可作为“通用底座”迁移到多任务/多领域的模型。
* **别名**：题库中常把大模型称为**基础模型**（强调可作为多任务、多应用的通用底座）。
* **典型能力谱系**：生成、理解、推理、工具使用（RAG/Agent）、多模态对齐等。

---

## 2. 发展脉络：从词向量到 Transformer
### 2.1 Word2Vec（词向量）
* **目标**：把离散词映射为带语义的**向量表示**，使相近语义在向量空间更接近。
* **两种经典训练目标**：
  * **Skip-gram**：中心词 → 预测上下文词（“用中心词找周围”）。
  * **CBOW**：上下文词 → 预测中心词（“用周围词猜中心”）。

### 2.2 Seq2Seq（机器翻译范式）
* **结构**：Encoder + Decoder。
* **历史背景**：2017 年前常用 **LSTM** 作为 encoder/decoder。
* **典型用途**：翻译、摘要、对话等“输入序列 → 输出序列”。

### 2.3 Transformer（Attention is All You Need）
* **组成**：Encoder + Decoder 两部分（经典版本）。
* **关键优势**：用注意力机制替代循环结构，训练并行度更高，适配大规模训练。

---

## 3. 三种主流架构：Encoder-Only / Decoder-Only / Encoder-Decoder
### 3.1 Encoder-Only（理解为主）
* **代表**：BERT（2018）。
* **训练特点**：随机 mask 一些词，让模型预测被 mask 的词（近似“完形填空/MLM”）。
* **适合任务**：分类、检索、匹配、信息抽取等。

### 3.2 Decoder-Only
* **代表**：GPT、Llama、DeepSeek 等主流大语言模型。
* **核心机制：因果掩码**
  * 自回归生成必须“只能看过去、不能看未来”，注意力矩阵严格上三角被屏蔽。
  * 常考题：长度为 $T$ 的序列，严格上三角元素个数为 $T(T-1)/2$。
* **适合任务**：文本生成、代码生成、对话、开放式问答等。

### 3.3 Encoder-Decoder
* **代表**：BART、T5。
* **适合任务**：翻译、摘要、结构化生成（输入理解后再生成输出）。

---

## 4. 训练范式：预训练 → 微调 →（可选）强化学习后训练
## 4.1 预训练：Next Token Prediction（NTP）
* **最常用框架**：**Next Token Prediction**。
* **监督信号的“右移一位”**：
  * 输入：$x_1, x_2, ..., x_T$
  * 目标：$x_2, x_3, ..., x_{T+1}$（等价理解为“整体右移一位”，长度通常不变）
* **损失**：常用交叉熵（Cross-Entropy）。
* **记忆点**：PPT强调“预训练很贵”（数据、算力、时间成本大）。

## 4.2 微调（Fine-tuning）：SFT 与 LoRA
* **目的**：在预训练模型基础上，用特定任务数据继续训练，使其在该任务上更好。
* **SFT（Supervised Fine-Tuning）**：监督学习式微调，是最常见微调方式之一。
* **全量微调 vs LoRA**
  * **全量微调**：更新预训练权重 $W$（参数多、显存/算力开销大）。
  * **LoRA（Low-Rank Adaptation）**：冻结 $W$，只训练低秩增量参数：
    * 常见表达：$h = Wx + BAx$（$A,B$ 为低秩矩阵，秩 $r < d$）。
  * **优势**：显著降低训练/存储/计算成本。

## 4.3 强化学习后训练：增强推理 & 对齐（Alignment）
* **增强推理能力（例：DeepSeek-R1-Zero）**
  * 在预训练模型（如 DeepSeek-V3）基础上，用强化学习增强推理能力。
  * 课堂强调现象：**生成文本长度逐渐增长**、并且**自发出现自我反思**。
* **对齐（Alignment）**
  * **目标**：让模型输出与**人类价值观/偏好/安全约束**一致（常与 RLHF 思想相关）。
  * 记忆点：PPT引用 Stuart J. Russell 的观点——我们追求“与人类价值一致的智能”，而非“纯粹更聪明”。

---

## 5. 提示词工程（Prompt Engineering）
* **定义**：通过“设计输入提示词”的方式影响模型输出质量与推理路径（不改权重）。
* **课堂经典例子**：同一个年龄题，直接问可能答错；先给几个示例再问，正确率显著提升。
* **常用策略清单（考试可写成要点）**：
  1. 明确角色/任务边界（你是谁、要做什么）。
  2. 给格式约束（必须输出 JSON / 表格 / 结论+理由）。
  3. 给少量示例（Few-shot）与反例（禁止什么）。
  4. 要求自检（给出核对步骤/用不同方法复算）。

---

## 6. 缩放定律（Scaling Law）与涌现能力
* **Scaling Law（幂律规律）**：在一定范围内，当其他因素不受限时，**测试损失**随
  * 计算量（Compute）
  * 数据量（Data）
  * 参数量（Params）
  的增加而呈现**幂律下降趋势**（Power law）。
* **容易考的“无关项”**：题库强调 scaling law 公式不包含“提示词长度”。
* **涌现能力（Emergent Abilities）**：随着规模增大，模型在某些任务的表现会从“接近随机”突然跃升到“显著高于随机”。

---

## 7. 上下文学习（In-context Learning）与思维链（Chain of Thought）
* **上下文学习（ICL）**：只给任务描述+少量示例，不做梯度更新、不改权重，模型也能适配新任务。
* **思维链（CoT）**：引导模型生成**中间推理步骤**，而不是直接给答案，可显著提升复杂任务表现。

---

## 8. 多模态基础：ViT 与 CLIP
### 8.1 ViT（Vision Transformer）
* **基本做法**：把图片切成小块（patch）当作 token，送入 Transformer **Encoder**。
* **记忆点**：常用表述 “An image is worth 16x16 words”。

### 8.2 CLIP（Contrastive Language–Image Pre-training）
* **训练方式**：对比学习（Contrastive Learning）。
* **机制**：文本和图片分别编码后对齐——匹配对相似度拉近、非匹配对推远。
* **范式归类**：更接近“度量学习”（metric learning）的思想。

---

## 9. 应用生态：RAG、Agent 与 MCP
* **RAG（Retrieval-Augmented Generation）**
  * 核心：先从外部知识库/搜索检索相关信息，再结合检索内容生成回答。
  * 常见收益：减少“无依据瞎编”的概率、提高可核验性。
* **AI Agent**
  * 关键能力：任务分解 + 工具调用 + 反馈闭环（从“会说”到“能做”）。
* **MCP（Model Context Protocol）**
  * 定位：统一大模型与外部数据源/工具之间通信的**开放协议标准**（解决“数据孤岛”与对接碎片化）。

---

## 10. 大模型推理：概念、类型、评估
### 10.1 推理能力的定义
* **推理能力**：模型从输入提取信息、进行逻辑推断并生成合理输出的能力；类比人类的假设验证、逻辑推导、归纳总结。

### 10.2 推理任务四大类型（题库口径）
1. **逻辑推理**
2. **数学推理**
3. **常识推理**
4. **因果推理**（强调因果链、干预与反事实）

### 10.3 推理能力评估方法
* **MMLU**：多任务语言理解（57 个领域多选题），评估知识覆盖与语言理解/推理。
* **GSM8K**：小学数学文字题（多步计算/推理）。
* **HumanEval**：编程题（看代码是否通过测试）。
* **Human Evaluation（人工评估）**：由人类从准确性、逻辑性、合理性等多个维度打分（不只看对错）。

---

## 11. 现象部分（课堂重点）：锚函数、凝聚与“推理 vs 记忆”
### 11.1 锚函数（Anchor Function）实验
* **设定**：序列中指定某个 token 为“锚点”，**损失只在锚点位置计算**，其他 token 作为“关键项”。
* **用途**：用来研究模型是在做“推理泛化”还是“死记硬背（memorization）”，并区分 ID（见过的锚点）与 OOD（没见过的锚点）。

### 11.2 凝聚（Condensation）现象
* **定义**：小初始化下，同层神经元训练过程中出现“趋同偏好”，训练后达到参数凝聚状态，使大网络在功能上**等价于小网络**。
* **本质**：一种**复杂度控制**（与“参数模长惩罚等正则化”起到类似效果）。

### 11.3 初始化 → 凝聚强度 → 推理/记忆的机制理解（PPT图的口径）
* **小初始化**：强凝聚 → 低复杂度 → 倾向只学少量“基元函数” → 更可能在“推理解上泛化”。
* **稍大初始化**：轻微凝聚 → 复杂度略高 → 更容易拟合更多函数（泛化变弱）。
* **很大初始化**：完全不凝聚 → 复杂度极大 → 容易“记住数据” → 完全不泛化。
* **题库常考判断**：很大初始化更容易死记硬背、泛化差；凝聚意味着大网络等价小网络。

### 11.4 记忆更容易发生在哪类关系？
* 在锚函数/机制理解相关题中：**无规律的随机映射**更容易靠记忆拟合，而非推理。

---

## 12. 风险与概念区分：幻觉 vs 偏见
* **幻觉（Hallucination）**：输出包含“貌似事实但虚假/误导”的信息；常发生在缺乏证据仍强行给确定答案时。
* **偏见（Bias）**：模型预测存在系统性倾向，带来公平性风险。
* **区分口诀**：  
  * 幻觉：**事实性错误/无依据**  
  * 偏见：**系统性倾向/公平性风险**

---

