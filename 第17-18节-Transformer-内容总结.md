# Transformer 架构 - 要点梳理

## 1. 核心概念：注意力机制
*   **定义**：一种允许模型在处理序列时，直接计算并整合序列中任意两个位置之间信息关联的机制。
*   **核心公式**：缩放点积注意力公式为 $\text{Attention}(Q, K, V) = \text{softmax}(\frac{QK^T}{\sqrt{d_k}})V$。
*   **核心优势**：
    *   **并行计算**：不同于RNN的串行处理，Transformer可以并行计算整个序列，极大提升训练效率。
    *   **长距离依赖**：注意力机制可以直接建模序列中任意距离的两个位置之间的关系，有效缓解RNN中的梯度消失/爆炸问题。

## 2. 关键组件与结构
*   **自注意力**：Query、Key、Value三个矩阵均来自同一输入源，用于捕捉序列内部各元素间的关系。
*   **多头注意力**：
    *   **原理**：将模型维度划分为多个“头”，让每个头在不同表示子空间中并行学习不同类型的依赖关系（如语法、指代关系）。
    *   **计算**：各头的输出拼接后，通过一个线性变换 $W_O$ 映射回模型维度。
*   **位置编码**：
    *   **作用**：由于注意力机制本身是位置无关的，需要显式地加入位置信息以区分词序。
    *   **常见类型**：
        *   **绝对位置编码（如正弦/余弦）**：为每个位置分配一个固定向量。
        *   **相对位置编码（如RoPE）**：编码元素间的相对距离，通常具有更好的长度外推能力。
*   **层归一化与残差连接**：
    *   **层归一化**：对每个token的特征维度进行标准化，稳定训练过程，通常采用Pre-LN结构（先归一化再进入子层）。
    *   **残差连接**：将子层输入直接加到其输出上（$y = x + f(x)$），有效缓解深层网络中的梯度消失问题。
*   **前馈网络**：逐位置独立作用的、由两个线性变换和一个激活函数组成的小型网络，用于增强模型的非线性表达能力。

## 3. 训练与解码
*   **训练目标**：
    *   **Next Token Prediction**：根据给定的上下文序列，预测下一个词（token），是自回归语言模型预训练的核心任务。
    *   **损失函数**：通常使用交叉熵损失，鼓励模型对真实的下一个词赋予更高的概率。
*   **解码策略**：
    *   **贪婪解码**：每一步都选择当前概率最高的词，效率高但可能不是全局最优。
    *   **Beam Search**：保留多个候选序列，旨在找到全局概率更高的输出。
    *   **因果掩码**：在解码器的自注意力中，使用上三角掩码（将未来位置设为负无穷）来防止模型“看到”未来的信息，保证生成的自回归特性。

## 4. 重要计算与性质
*   **注意力分数缩放**：计算点积 $QK^T$ 后除以 $\sqrt{d_k}$，目的是防止点积结果过大导致Softmax梯度趋于平缓。
*   **Softmax性质**：
    *   输出之和恒为1。
    *   对输入整体加上一个常数（平移不变性），输出不变。
*   **LayerNorm计算**：对单个样本的所有特征维度计算均值和方差，并进行标准化。

## 5. 模型参数与维度
*   **维度关系**：在多头注意力中，若模型维度为 $d_{model}$，头数为 $h$，则每个头的维度通常为 $d_k = d_v = d_{model}/h$。
*   **参数计算**：主要参数集中在注意力层的 $W^Q, W^K, W^V, W^O$ 矩阵和前馈网络的两层线性变换中。