# 第1-7周知识要点总结

## 一、深度学习基础

### 1. 深度学习的三大驱动力
- **大数据**：海量标注数据（如 ImageNet）
- **高算力**：GPU 等硬件提升
- **算法优化**：网络结构、训练技巧、激活函数等改进  

### 2. 神经元与感知机
- **神经元核心功能**：对不同输入进行**加权求和 + 偏置**，再经过非线性激活函数。
- **单层感知机局限**：只能处理**线性可分**问题（如无法解决 XOR）。
- **多层网络的优势**：通过非线性变换可将线性不可分数据变得线性可分。

### 3. 激活函数
- **Sigmoid**：存在**饱和区**（输入过大或过小时梯度趋近 0），导致**梯度消失**，深层网络训练困难。
- **ReLU**：`max(0, x)`，缓解梯度消失，计算简单。
- **Softmax**：将 logits 转换为**概率分布**（输出和为 1，每个值在 [0,1]），用于多分类。

### 4. 万有逼近定理（Cybenko 1989）
- **单隐层 + 非线性、连续、非多项式激活函数**的网络可以逼近任意连续函数。

### 5. 神经网络结构与参数计算
- 示例：输入 10 维 → 隐藏层 100 个神经元（带偏置）→ 输出层 10 个神经元（无偏置）  
  参数量 = \(10\times100 + 100 + 100\times10 = 2100\)

### 6. 反向传播（BP）算法
- 1986 年由 Rumelhart 和 Hinton 等人完善推广。
- 核心：**链式法则**，将输出端误差逐层反向传播，计算各层参数的梯度。

### 7. 参数初始化
- 所有权重初始化为相同非零值 → 同一层神经元学习相同特征（对称性破坏）。
- 模型越大，初始化取值通常越小。

### 8. 残差连接（Residual Connection）
- 加法形式 `y = x + F(x)` 可缓解梯度消失（恒等映射保留梯度）。
- 乘法形式 `y = x * F(x)` 无法缓解梯度消失（梯度中会乘以 `F(x)`，饱和时消失）。

### 9. 交叉熵损失函数
- 用于**分类任务**，公式：\(-\sum_i y_i \log p_i\)（one-hot 标签下简化为 \(-\log p_{\text{target}}\)）。

---

## 二、机器学习核心概念

### 1. 监督学习 vs 无监督学习
- **监督学习**：训练数据包含**标签**，学习输入-输出映射。
- **无监督学习**：训练数据**无标签**，挖掘数据内在结构（如聚类、降维）。

### 2. 过拟合与正则化
- **过拟合**：训练集表现好，测试集差；通常由于模型过于复杂或训练时间过长。
- **正则化**：在损失函数中加入惩罚项。
  - \(L_1\) 范数：各元素绝对值之和，可产生稀疏解。例：`[6, -8]` 的 L1 范数为 14。
  - \(L_2\) 范数：平方和开方。
- **决策树剪枝**：降低复杂度，提高泛化能力（而非追求训练精度）。

### 3. 模型评估与数据集划分
- **训练集**：学习参数。
- **验证集**：调整超参数、监控过拟合。
- **测试集**：评估最终泛化能力。
- **泛化能力**：用**测试误差**衡量，而非训练误差。

### 4. 损失函数与优化目标
- **0-1 损失**：不连续、不可导，难以梯度优化。
- **交叉熵损失**：连续可导，适合分类。
- **最小二乘法**：用于线性回归，几何本质是将目标向量 **投影到设计矩阵的列空间**。

### 5. 支持向量机（SVM）
- **点到超平面的几何距离**：\(r = \frac{|w^T x + b|}{\|w\|}\)
- **软间隔参数 C**：C 越大 → 对误分类惩罚越重 → 更少训练误差，但间隔可能变小。
- **支持向量**：距离分类超平面**最近**的样本点（决定边界）。
- **核技巧**：将低维线性不可分数据隐式映射到**高维空间**，使其线性可分。

### 6. 多分类策略
- **One vs One (OvO)**：对任意两类训练一个分类器，6 分类需 \( \binom{6}{2}=15 \) 个分类器。

---

## 三、优化方法

### 1. 梯度下降
- 参数更新公式：\(\theta_{t+1} = \theta_t - \eta \nabla J(\theta_t)\)（沿负梯度方向最小化损失）

### 2. 学习率
- 是**超参数**，需手动设置（通常基于验证集调整）。
- 过大：损失震荡甚至发散；过小：收敛缓慢。

### 3. 动量法（Momentum）
- 利用**历史梯度的移动平均**，加速收敛，减少峡谷地形中的震荡。

---

## 四、数学基础

### 1. 线性代数
- **线性无关**：只有全零系数才能使线性组合为零向量。
- **正交矩阵**：\(Q^{-1} = Q^T\)。
- **奇异值分解 (SVD)**：\(A = U\Sigma V^T\)，\(\Sigma\) 对角线上奇异值**非负**。
- **范数**：
  - \(L_1\)：绝对值之和
  - \(L_2\)：欧几里得距离
  - \(L_\infty\)：向量元素绝对值最大值

### 2. 高维几何与维数灾难
- **角度集中**：高维空间中随机两个向量几乎**正交**（夹角趋近 90°）。
- **距离集中**：所有点对距离趋于相等 → 基于距离的分类算法（如 KNN）效果变差。
- **体积集中**：高维单位球体积集中在**表面薄壳**（如 1000 维中半径 0.99 的内球体积不到 0.01%）。
- **数值积分**：所需网格点数随维度**指数增长**（如 20 维每维 2 等分 → \(2^{20} \approx 10^6\) 点）。
- **蒙特卡洛方法**：误差与维度**无关**（仅依赖样本数 n 和方差），可克服维数灾难。

### 3. 高维数据示例
- 图像、文本 one-hot 编码、围棋棋盘状态 → 高维
- 一维时间序列单变量记录 → 低维

---

## 五、降维与可视化

### 1. PCA（主成分分析）
- **线性降维**方法。
- **第一主成分**：投影后方差最大的方向（最大限度保留数据离散程度）。
- 各主成分彼此**正交**（线性无关）。
- PCA 是线性变换，无法捕捉复杂非线性结构。

### 2. t-SNE
- **非线性降维**，在 MNIST 等数据集上可视化效果明显优于 PCA（能清晰分离不同类别）。

### 3. 降维的本质
- 在低维子空间中**保留样本间的关联信息**（而非简单删除特征或等权平均）。

---

## 六、聚类算法

### 1. K-means
- 需**预先指定簇数 k**，无法动态调整。
- 收敛条件：各簇的**均值向量**不再变化。
- 局限性：对初始值敏感，只能发现球形簇。

### 2. 高斯混合模型（GMM）
- 样本属于某簇的依据：**后验概率**（而非欧氏距离）。
- 同样需要预先指定簇数 k。

---

## 七、决策树

- 通过**递归划分**样本空间构建。
- **持续划分至纯度为最大** → 过拟合。
- **多变量决策树**：可产生非轴对齐的划分边界，但学习难度和过拟合风险更高。

---

## 八、其他重要概念

### 1. 线性判别分析（LDA）
- 目标：**最大化类间距离**，**最小化类内距离**。

### 2. 随机投影引理
- 目标维度 \(k = O(\log n)\)，与**样本数 n 的对数**有关，**与原始维度 d 无关**。

### 3. 数值积分中的维数灾难
- 所需节点数随维数**指数增长**，传统方法（有限差分、有限元）在高维（如 100 维）不可行。

### 4. 高维立方体对角线
- 边长为 1 的 d 维立方体，对角线长度 = \(\sqrt{d}\)（d=100 时为 10）。

### 5. 高维球体内两点距离
- 当维度很大时，球内任意两点的距离约为 \(\sqrt{2}\)（趋近于正交）。

