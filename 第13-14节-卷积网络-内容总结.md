# 卷积神经网络 - 要点梳理

## 1. 核心概念：图像数据的空间特性
*   **局部关联性**：图像数据在空间上表现出局部关联特征，距离越近的像素点关联强度越高，这是CNN能够有效处理图像的基础。
*   **关联衰减模型**：图像中两个点的关联强度随距离增加而下降，通常遵循指数衰减或幂级数衰减规律。幂级数衰减可以理解为无穷多个半衰期的指数衰减之和，具有"无穷慢衰减"的成分。
*   **图像不变性**：图像数据集具有平移不变性、旋转不变性和伸缩不变性，但不具有置换不变性（像素顺序很重要）。

## 2. 卷积操作的核心机制
*   **权重共享**：同一个卷积核在整个图像上滑动时共用一组参数，这是CNN相比全连接网络大幅减少参数量的关键机制。权重共享并不意味着只有一组参数，而是卷积核内的参数在整个空间位置共享。
*   **局部感知**：卷积核每次只处理图像的一个局部区域，对应生物视觉系统中的感受野特性。卷积核越大，感受野越大，提取的特征越宏观；卷积核越小，提取的特征越局部。
*   **多通道卷积**：卷积核的深度必须与输入数据的通道数保持一致（如RGB图像需要3通道卷积核），输出特征图的通道数（深度）等于该层卷积核的个数。每个输出通道共享一个偏置值，因此偏置参数量等于输出通道数。

## 3. 卷积层的基本操作
*   **填充（Padding）**：通过在图像边缘补零，可以抵消卷积导致的尺寸收缩。当填充 $P = (K-1)/2$（$K$ 为卷积核大小）且步长为1时，输出尺寸与输入相同。填充不增加可学习参数，只是补零操作。
*   **步长（Stride）**：定义卷积核在图像上每次滑动的像素距离。步长大于1会导致输出特征图尺寸变小，实现下采样。步长为2的卷积可以替代池化层实现可学习的下采样。
*   **输出尺寸计算**：公式为 $(N + 2P - K) / S + 1$，其中 $N$ 为输入尺寸，$P$ 为填充，$K$ 为卷积核大小，$S$ 为步长。若使用填充 $P = (K-1)/2$ 且步长为1，则输出尺寸等于输入尺寸。
*   **参数量计算**：每个卷积核的参数量（权重）= 卷积核大小 × 输入通道数。总参数量 = 卷积核数量 × 每个卷积核参数量 + 输出通道数（偏置）。

## 4. 池化层与下采样
*   **最大池化**：取区域内的最大值，保留最强信号，能更好地保留纹理的亮度特征，增强模型对物体位置轻微变动的容忍度（平移不变性）。
*   **平均池化**：计算区域内所有值的平均值，平滑特征，有助于减少噪声影响。
*   **全局平均池化**：将整个特征图池化为单个值，每个通道输出一个值。可以直接替代全连接层，大幅减少参数量并防止过拟合。
*   **池化输出尺寸**：公式为 $(输入尺寸 - 池化窗口大小) / 步长 + 1$。池化不改变通道数，只降低空间分辨率。

## 5. 经典网络架构
*   **LeNet-5**：由Yann LeCun于1998年提出，用于识别手写数字的早期经典CNN，是现代CNN的奠基之作。标准CNN通常由卷积层、池化层、全连接层和输出层构成。
*   **AlexNet**：2012年ImageNet竞赛冠军，相比LeNet-5的重大改进包括使用ReLU激活函数、GPU加速训练、引入Dropout等正则化技术，以及更深的网络结构。
*   **网络结构演变**：通过卷积层提取特征，Flatten操作将最后一个池化层的多维特征图拉伸为一维向量，输入到全连接分类器。CNN相比全连接网络最显著的优势是能够利用图像的空间几何结构。

## 6. 现代CNN技术
*   **批归一化（Batch Normalization）**：通过标准化每层的输入分布，加速训练并提高稳定性，允许使用更大的学习率。训练阶段使用当前批次的统计量，推理阶段使用训练时计算的移动平均统计量。
*   **残差连接**：通过跳跃连接传递梯度，缓解深层网络中的梯度消失和网络退化问题，使网络能够训练得更深。
*   **深度可分离卷积**：将标准卷积分解为深度卷积（每个通道独立）和逐点卷积（1×1卷积融合通道）两步，显著减少参数量和计算量，但感受野与标准卷积相同。
*   **1×1卷积**：不改变特征图的空间尺寸，但可以在通道维度上进行信息融合和降维，常用于跨通道信息融合。
*   **转置卷积**：通过反向的卷积操作实现上采样，增大特征图尺寸，常用于语义分割、生成模型等需要增大特征图尺寸的任务。
*   **空洞卷积（Dilated Convolution）**：通过在卷积核元素之间插入空洞来扩大感受野，在不增加参数的情况下扩大感受野，常用于密集预测任务。
*   **组卷积（Group Convolution）**：将输入通道分组，每组独立进行卷积操作，参数量减少到标准卷积的1/组数，同时保持一定的表达能力。
*   **注意力机制**：使网络能够自适应地关注重要的特征区域。通道注意力关注不同通道特征的重要性，空间注意力关注空间位置的重要性。

## 7. 网络设计原则
*   **多层小卷积核替代大卷积核**：两个3×3卷积核堆叠的参数量（18）小于一个5×5卷积核（25），且感受野相同，同时增加了非线性激活，提高表达能力。
*   **步长卷积替代池化**：步长为2的卷积可以实现下采样，同时具有可学习的参数，相比固定池化操作可以保留更多有用信息。
*   **特征图尺寸变化规律**：通过卷积和池化操作，CNN通常逐层降低特征图的空间分辨率，同时增加通道数和语义抽象程度。若所有卷积层都使用填充为1、步长为1的3×3卷积，则无论网络多深，特征图的空间尺寸都不会改变。

## 8. 多尺度特征融合
*   **特征金字塔网络（FPN）**：通过融合不同层级的特征，使网络能够同时处理不同尺度的目标。浅层特征包含更多的细节信息（如边缘、纹理），深层特征包含更多的语义信息（如物体类别、形状）。
*   **密集连接**：使每一层都能直接访问前面所有层的特征，促进了特征重用和梯度流动，提高了网络效率。
*   **特征重用**：通过跳跃连接或多路径结构，浅层的细节特征可以被深层网络复用，提高特征利用效率。

## 9. 训练技巧与正则化
*   **Dropout**：在训练时随机使神经元失效，防止模型过度依赖局部神经元，提高泛化能力，是一种有效的正则化手段。
*   **数据增强**：通过对训练数据进行变换（旋转、翻转、缩放、裁剪、颜色变换、噪声添加等），增加数据多样性，提高模型的泛化能力。
*   **迁移学习**：利用在大数据集上预训练的模型，通常只需要微调部分层（如最后几层），前面的层可以冻结使用预训练权重，大大减少训练时间和数据需求。
*   **学习率衰减**：在训练初期使用较大学习率快速收敛，后期使用较小学习率精细调整，提高最终模型精度。
*   **梯度裁剪**：通过限制梯度的大小，防止梯度爆炸导致的训练不稳定，常用于训练深层网络。
*   **权重初始化**：合适的权重初始化主要影响训练初期的梯度流动和收敛速度，对训练稳定性至关重要。

## 10. 重要计算与性质
*   **卷积输出尺寸**：$(N + 2P - K) / S + 1$，其中 $N$ 为输入尺寸，$P$ 为填充，$K$ 为卷积核大小，$S$ 为步长。
*   **池化输出尺寸**：$(输入尺寸 - 池化窗口大小) / 步长 + 1$。
*   **参数量计算**：卷积层参数量 = 卷积核数量 × 卷积核大小 × 输入通道数 + 输出通道数（偏置）。全连接层参数量 = 输入维度 × 输出维度 + 输出维度。
*   **感受野**：表示输出特征图上的一个像素点对应输入图像上的区域大小，随着网络深度增加而增大，反映了网络能够"看到"的输入范围。

## 11. 模型诊断与优化
*   **过拟合**：训练集表现好但测试集表现差，说明模型过度学习了训练数据的特征。解决方法包括增强正则化（Dropout、权重衰减）、数据增强、减少模型复杂度。
*   **欠拟合**：训练集和验证集都表现不佳，可能是模型容量不足、训练不充分、学习率设置不当等。解决方法包括增加模型容量、延长训练、调整学习率。
*   **损失不下降**：可能的原因包括学习率过小、梯度消失、模型容量不足、数据问题等，需要综合分析多个因素。

## 12. 特殊网络结构
*   **全卷积网络（FCN）**：不使用全连接层，可以处理任意尺寸的输入图像，常用于语义分割等密集预测任务。
*   **自适应池化**：可以根据输入尺寸自动调整池化窗口大小，确保输出为固定尺寸，使网络能够处理不同大小的输入。
*   **可变形卷积**：通过学习偏移量来调整采样位置，使卷积核能够自适应地调整感受野形状，更好地适应目标几何变换。
