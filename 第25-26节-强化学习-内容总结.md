# 强化学习 - 要点梳理

## 1. 强化学习问题形态（RL vs 监督/无监督）
### 1.1 核心定义与要素
- RL = **序列决策 + 交互 + 奖励反馈**：智能体在动态环境中选动作，通过试错最大化长期回报。
- 关键闭环：$(s_t, a_t, r_{t+1}, s_{t+1})$。

### 1.2 与监督/无监督对比
- 监督学习：给定 $(x,y)$ 预测 $y$（无交互、无长期回报）。
- 无监督学习：无标签（聚类/降维），目标是结构发现而非累计奖励。
- 强化学习：数据分布受**策略影响**（行为会改变未来看到的数据），并以累计奖励为目标。

### 1.3 典型题型
- 给场景判断是否 RL；指出 RL 的三个要素（交互、动作、奖励）。
- 给一段过程，圈出 $s,a,r,s'$ 的对应关系。



## 2. 可观测性 / 动作空间 / 任务类型
### 2.1 完全可观测 vs 部分可观测
- **完全可观测**：观测 $o_t$ 与真实状态 $s_t$ 等价/可恢复（信息完整）。
- **部分可观测（POMDP）**：$o_t$ 是 $s_t$ 的子集/带噪投影（信息不完整）。
  - 常见补足方式：使用历史 $h_t=(o_1,a_1,\dots,o_t)$，或信念状态 $b_t=P(s_t\mid h_t)$。

**高频辨析点**
- “部分可观测环境中观察信息完整”→ 错。
- “完全可观测意味着 observation = state”→ 对（或可一一映射/可恢复）。

### 2.2 动作空间
- **离散动作**：通常有限动作集合（“离散=无限”是错）。
- **连续动作**：动作取连续值（如转向角、油门、机械臂力矩）。
- 题型：判断某任务更像离散还是连续；说明连续动作更难直接 $\arg\max$、常需策略梯度/actor-critic（若课程覆盖）。

### 2.3 任务类型
- **情节型 Episodic**：有终止状态，episode 有限长。
- **连续型 Continuing**：通常无终止状态；用折扣回报/平均回报定义目标（“连续型一定有终止”是错）。

**常考能力**
- 给奖励序列与 $\gamma$，计算 $G_0$（终点奖励折扣到现值）。
- 区分“回合结束才能算回报”的 MC 与可在线更新的 TD。



## 3. 马尔可夫性与 MDP
### 3.1 马尔可夫性
- **马尔可夫性**：
  $$P(s_{t+1}\mid s_t,a_t,\text{history})=P(s_{t+1}\mid s_t,a_t).$$
- 直观：当前状态已经包含做最优决策所需的全部历史信息。

### 3.2 MDP 五元组与含义
- **MDP**：$(\mathcal S,\mathcal A,P,R,\gamma)$
  - $\mathcal S$：状态空间
  - $\mathcal A$：动作空间
  - $P(s'\mid s,a)$：转移概率
  - $R(s,a)$ 或 $R(s,a,s')$：奖励
  - $\gamma$：折扣因子


## 4. 回报与折扣
### 4.1 折扣回报定义
- 情节型回报：
  $$G_t=\sum_{k=0}^{T-t-1}\gamma^k r_{t+k+1}.$$
- 连续型无穷折扣和（每步奖励恒为 1）：
  $$\sum_{k=0}^{\infty}\gamma^k=\frac{1}{1-\gamma}\quad(\gamma<1).$$

### 4.2 折扣直觉与常见陷阱
- $\gamma$ 越大越重视长期；远期奖励权重降低（不确定性/时间偏好）。
- 远期“大额奖励 + 小概率”比较题：期望折扣回报
  $$\mathbb E[G]=p\cdot \gamma^{n}\cdot R.$$


## 5. 价值函数与策略
### 5.1 策略（policy）
- 策略：$\pi(a\mid s)$，在状态 $s$ 下选动作的规则/分布（确定性/随机性）。

### 5.2 价值函数定义与关系（常考推导）
- 状态价值：$V^\pi(s)$；动作价值：$Q^\pi(s,a)$。
- 关系：
  $$V^\pi(s)=\sum_a \pi(a\mid s)\,Q^\pi(s,a).$$
- 一步动作价值（已知后继价值时常用）：
  $$Q(s,a)=r(s,a)+\gamma V(s').$$


## 6. Bellman 方程
### 6.1 核心思想
- 长期价值 = 一步奖励 + 折扣后的后继价值期望（递推/自洽方程）。

### 6.2 期望形式（评估固定策略或无动作）
- 期望形式：
  $$V(s)=\mathbb E[r+\gamma V(s')].$$

### 6.3 最优 Bellman 备份（值迭代）
- 最优备份：
  $$V_{\text{new}}(s)=\max_a\{r(s,a)+\gamma \mathbb E[V(s')]\}.$$


## 7. DP / MC / TD 三大类算法
### 7.1 动态规划（DP）
- 前提：已知模型 $P(s'\mid s,a)$（常作为限制点）。
- 典型：价值迭代、策略迭代。
- 高频考点：DP 是“用模型算期望”，不是靠采样。

### 7.2 蒙特卡洛（MC）
- 完整回合回报样本均值估计：
  $$\hat Q(s,a)=\frac{1}{N}\sum_{i=1}^N G^{(i)}.$$
- 特性：无偏（在常见设定下）但方差大；通常需 episode 结束。
- 高频题型：给多次回报样本，做均值估计。

### 7.3 时序差分（TD）
- 关键：**采样 + 自举（bootstrapping）**。
- TD 误差：
  $$\delta=r+\gamma V(s')-V(s).$$
- TD(0) 更新：
  $$V(s)\leftarrow V(s)+\alpha\delta.$$
- 高频题型：给 $V(s),r,V(s')$，算 $\delta$，再代入一次更新。

### 7.4 DP/MC/TD 对比
- 是否需要模型：DP 要；MC/TD 不一定要。
- 是否需要等回合结束：MC 通常要；TD 不需要。
- 偏差-方差直觉：MC 方差较大；TD 有自举引入偏差但更高效。


## 8. On-policy vs Off-policy
### 8.1 概念
- **On-policy**：采样策略 = 更新目标策略（Sarsa）。
- **Off-policy**：采样策略 $\ne$  更新目标策略（Q-learning，用 $\max$ 目标）。

### 8.2 直觉与常考解释
- Sarsa 更新目标使用实际执行的 $a'$：更贴近“包含探索的行为”，更保守。
- Q-learning 用 $\max_{a'}$：假设未来总选最优动作，目标更激进；在存在风险/惩罚时可能更“高风险但更优”。


## 9. 经典更新公式
### 9.1 Sarsa（On-policy）
- 目标：
  $$\text{Target}=r+\gamma Q(s',a').$$
- 更新：
  $$Q(s,a)\leftarrow Q(s,a)+\alpha\big(r+\gamma Q(s',a')-Q(s,a)\big).$$

### 9.2 Q-learning（Off-policy）
- 目标：
  $$\text{Target}=r+\gamma\max_{a'}Q(s',a').$$
- 更新：
  $$Q(s,a)\leftarrow Q(s,a)+\alpha\big(r+\gamma\max_{a'}Q(s',a')-Q(s,a)\big).$$

### 9.3 典型计算流程
1. 写目标 Target（Sarsa 或 Q-learning）。
2. 写误差：Target $- Q(s,a)$。
3. 乘学习率 $\alpha$。
4. 加回原值：$Q \leftarrow Q + \alpha(\text{Target}-Q)$。

## 10. 探索-利用与 $\epsilon$ -greedy
### 10.1 $\epsilon$ -greedy 定义
- 以 $1-\epsilon$ 选贪心动作，否则均匀随机选动作。

### 10.2 常用概率与期望
- 动作数 $|\mathcal A|=n$，贪心动作概率：
  $$P(a^*)=(1-\epsilon)+\frac{\epsilon}{n}.$$
- 期望动作价值：
  $$\mathbb E[Q]=(1-\epsilon)\,Q_{\max}+\epsilon\cdot \frac{1}{n}\sum_{i=1}^n Q_i.$$

### 10.3 高频辨析点
- 探索不足：可能陷入次优（遗漏高回报区域）。
- 探索会降低当下期望但提升长期最优概率（“短期损失换信息”）。


## 11. DQN（深度 Q 学习）与“为什么要深度”
### 11.1 表格法的规模问题
- 表格 Q 的参数量：
  $$|\mathcal S|\times|\mathcal A|.$$
- 题型：给 $|\mathcal S|,|\mathcal A|$，算需要存多少个数（如 $10^6\times 10$）。

### 11.2 DQN 的核心改变
- 用神经网络近似 $Q(s,a;\theta)$ 替代显式表格：
  - 参数共享：减少存储与样本需求
  - 泛化能力：相似状态共享表示
  - 适配高维输入（像素/传感器）


## 六个关键公式
1) 回报：$G_t=\sum_{k}\gamma^k r_{t+k+1}$  
2) Bellman：$V=\mathbb E[r+\gamma V']$；最优备份：$V_{\text{new}}=\max_a(r+\gamma \mathbb E[V'])$  
3) 状态-动作关系：$V^\pi(s)=\sum_a\pi(a|s)Q^\pi(s,a)$  
4) TD：$\delta=r+\gamma V'-V$，$V\leftarrow V+\alpha\delta$  
5) Sarsa：$Q\leftarrow Q+\alpha(r+\gamma Q'-Q)$（On-policy）  
6) Q-learning：$Q\leftarrow Q+\alpha(r+\gamma\max Q'-Q)$（Off-policy）



## 附：高频“会做题”清单（对应你题库题型）
- 折扣回报（3 步/多步/含负奖励/无穷和）。
- Bellman 两状态联立方程（写方程→消元→表达式）。
- 一步最优备份（值迭代）与策略加权（策略评估）。
- TD(0)/Sarsa/Q-learning 一次更新（Target→Error→Update）。
- $\epsilon$ -greedy 的贪心概率与期望 $Q$ 计算。
- 表格规模估算与 DQN 选择理由。
