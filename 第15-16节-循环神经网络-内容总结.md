# 循环神经网络 (RNN) 与自然语言处理 - 要点梳理

## 1. 自然语言处理 (NLP) 基础概念
* **定义与目标**：
    * 使计算机能够理解、生成和处理人类自然语言，从而实现人机之间更加自然和高效的交互。
    * **核心应用**：机器翻译、语音识别、文本分类、信息检索、智能客服等。
* **语言数据的独特性**：
    * **离散符号**：语言是离散的符号序列，不同于图像的连续像素值。
    * **层次与递归**：具有严格的语法结构（主谓宾）和递归性（从句套从句，如“他说他昨天...”）。
    * **长程依赖**：理解当前词往往需要依赖上下文甚至很远的前文。
    * **上下文相关性**：词义取决于语境（例：“上海的交通大学” vs “上海的交通”）。
* **数据预处理三要素**：
    * **质量过滤**：基于规则（如点赞数）或分类器清洗低质数据 。
    * **去重**：重复数据会降低模型的上下文处理能力，需在句子、文档或数据集级别去重。
    * **隐私与安全**：过滤有毒内容及个人身份信息 。

## 2. 词元化 (Tokenization)
* **定义**：将文本切分为语义或语法单元（Token）的过程。
* **主要方法对比**：
    * **基于规则**：按空格或标点切分，简单但在处理复杂词汇时能力有限。
    * **基于统计 (重点)**：统计语言中单词或子词的频率来决定切分边界，常用方法包括 **BPE**, **WordPiece**, **Unigram**。
* **BPE (Byte Pair Encoding) 算法机制**：
    * **流程**：从基本符号（如字母）开始，迭代统计相邻词元对的共现频率，将**频率最高**的一对合并为新词元。
    * **示例**：
        1.  初始语料频率最高的是 "oo"，合并为 "oo"。
        2.  下一轮 "l" 和 "oo" 频率最高，合并为 "loo"。
* **特殊 Token**：
    * `CLS` (句首/分类)、`SEP` (句尾/分隔)、`MASK` (掩码)、`PAD` (填充)、`UNK` (未登录词)。

## 3. 词表示与嵌入 (Embeddings)
* **独热编码 (One-hot)**：
    * **形式**：高维稀疏向量 $\varepsilon_i \in \mathbb{R}^N$，仅一位为1。
    * **缺陷**：维度灾难（如100万维），且任意两词正交，无法捕捉语义相似度（距离相等）。
* **词嵌入 (Word Embedding)**：
    * **定义**：将词映射到低维稠密向量空间，$x_i = W_{emb} \varepsilon_i$。
    * **数学支撑**：**Johnson-Lindenstrauss 引理**——高维点投影到低维空间（$k \sim \log n$）可近似保持点间距离。
    * **语义特性**：向量距离反映语义关系，如 $Dist(study, learn) < Dist(study, play)$。

## 4. 训练框架：Next Token Prediction
* **核心逻辑**：基于前文序列预测下一个 Token ($x_{n+1}$)，即 $P(x_{n+1}|x_1, ..., x_n)$。
* **优势**：
    * **自监督学习**：无需人工标签，任何文本均可作为训练数据（解决了标签昂贵的问题）。
    * **上下文感知**：生成的文本具有连贯性和逻辑性。
* **损失函数**：通常使用**交叉熵损失 (Cross-Entropy Loss)**。

## 5. 循环神经网络 (RNN)
* **结构特点**：
    * 引入**状态记忆** $S_t$，处理时序信息。
    * **参数共享**：在所有时间步中，输入权重 $\mathbf{U}$、循环权重 $\mathbf{W}$ 和输出权重 $\mathbf{V}$ 保持不变。
* **前向传播**：
    * $S_t = \tanh(\mathbf{U} \cdot x_t + \mathbf{W} \cdot S_{t-1} + b)$ 。
    * $o_t = \text{Softmax}(\mathbf{V} \cdot S_t)$。
* **训练问题 (BPTT)**：
    * **BPTT**：随时间反向传播，梯度计算涉及时间步的连乘。
    * **梯度消失/爆炸**：由于权重矩阵 $\mathbf{W}$ 的连乘效应，梯度容易在长序列中消失或爆炸，导致难以捕捉长程依赖。

## 6. 长短期记忆网络 (LSTM)
* [cite_start]**设计动机**：解决 RNN 的梯度消失问题，有效捕捉长程依赖 [cite: 215]。
* **核心机制：门控 (Gating)**：
    * **遗忘门 ($f_t$)**：决定保留多少旧记忆 ($C_{t-1}$) 。
    * **输入门 ($i_t$)**：决定写入多少新信息。
    * **输出门 ($o_t$)**：决定当前时刻输出多少隐状态信息。
* **双状态系统**：
    * **Cell State ($C_t$)**：记忆状态。更新公式 $C_{t+1} = f_t * C_t + i_t * \tilde{C}_{t+1}$。
        * **ResNet 类比**：Ilya Sutskever 将 LSTM 比作旋转 90° 的 ResNet，信息可通过加法路径无损传播。
    * **Hidden State ($S_t$)**：隐状态。$S_{t+1} = o_t * \tanh(C_{t+1})$。

## 7. 神经网络的可解释性
* **RNN 的可解释性实验** (Karpathy et al.)：
    * RNN 内部会自动学习出具有特定功能的神经元。
    * **引用检测单元**：在引号内的文本段落中保持活跃。
    * **行长追踪单元**：随着一句话的进行，激活值逐渐衰减（感知换行时机）。
    * **代码结构单元**：在 `if` 语句等代码结构中表现出特定的激活模式。

## 8. 进阶应用：图像描述 (Image Captioning)
* **架构**：**Encoder-Decoder** 结构。
    * **Encoder**：使用 CNN (如 VGG) 提取图像特征。
    * **Decoder**：使用 RNN/LSTM 生成描述文本。
* **注意力机制 (Attention)**：
    * **原理**：在生成每个词时，模型动态计算图像不同区域的权重（Attention Map），即模型在“看”哪里。
    * **类型**：
        * **Soft Attention**：对所有区域特征进行加权求和（确定性，可微分）。
        * **Hard Attention**：随机采样某个区域（随机性）。

## 9. 实验与数据集
* **IMDB 数据集**：
    * **任务**：情感分析（二分类，正面/负面） 。
    * **规模**：50,000 条影评（25k 训练 + 25k 测试），正负各半。
* **Kaggle 实验**：
    * 对比 MLP, CNN, LSTM 及 Transformer 在分类任务上的性能。
